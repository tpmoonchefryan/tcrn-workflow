<!-- tcrn-doc-synced-to: CONTRIBUTING.md 0309c55144aa0e4cdbdaab6290001650e3c0c805c3235bd767940a2934ddf93e -->

> **영어가 권위 있는 버전입니다.** 이 번역은 편의를 위해 제공됩니다. 둘이 다를 경우 CONTRIBUTING.md의 영어 텍스트가 우선합니다.

# 기여

고정된 Node와 pnpm 버전을 사용하십시오. 패키지 라이프사이클 스크립트를 켜거나, 고정되지 않은 실행 파일을 추가하거나, 텔레메트리를 도입하거나, 프로젝트 명령이 암묵적으로 네트워크에 접근하게 만들지 마십시오.

변경을 제안하기 전에:

```sh
pnpm install --offline --frozen-lockfile --ignore-scripts
pnpm verify:p1
```

의존성은 정확한 버전이어야 하고, Apache-2.0 배포와 호환되어야 하며, 오프라인 의존성 정책과 취약점 정책에 추가되어야 합니다. 주석을 허용하는 소스 파일은 `SPDX-License-Identifier: Apache-2.0`을 포함해야 합니다.

릴리스 동작은 외부 신뢰 루트가 부재하거나, 후보가 통제하거나, 만료되었거나, 폐기되었거나, 서명된 매니페스트와 불일치할 때 페일클로즈해야 합니다.

## 증명 예산

**정의.** 증명 질량은 `tests/**/*.mjs`와 `scripts/**/*.mjs`의 총 개행 수입니다(`.mjs` 필터는 `scripts/policy` JSON 정책 파일을 제외합니다). 제품 질량은 `packages/*/src/**/*.ts`의 총 개행 수입니다. 빈 줄과 주석은 의도적으로 계산에 넣습니다: 이 측정은 일부러 조악하게 만들어, 결정적이고 재포매팅 논쟁의 여지가 없게 합니다. 증명 대 제품 비율은 `proofMass / productMass`입니다.

**규칙.** 비율이 `1.0` 이상인 동안에는, 어떤 풀 리퀘스트도 새로운 검증 게이트 — 즉 새 `scripts/task.mjs` 핸들러, 새 `verify:*` 스크립트, 또는 카테고리가 `framework-hygiene`인 새 verification-map 주장 — 를 도입할 수 없습니다. 다만 같은 풀 리퀘스트가 최소한 동등한 증명 질량을 폐기하거나, Owner가 서면 예외를 기록하는 경우는 예외입니다. 카테고리가 `runtime-capability`인 주장은 면제됩니다: 그것들은 제품이 제 일을 하는 것이지 증명용 비계가 아닙니다.

**기준선.** 채택 시점에 측정된 비율은 약 `1.62`였고(정의에 따라 `packages/protocol` 패키지를 제품 질량에 포함한 정정 기준선), `1.0` 임계값을 훨씬 웃돌므로 규칙이 구속력을 가집니다.

**기록된 예외 — OD-21, 2026-07-19, `pnpm guard-check`.** Owner는 가드 레지스트리와 그 변이 검사기(`scripts/guard-check.mjs`, `scripts/policy/guard-registry.json`)에 대해 서면 예외 하나를 부여합니다.

이 예외는 회피되지 않고 여기에 기록되며, 그 구분이 중요합니다. 이 검사기는 독립 실행형 npm 스크립트로 — `push-gate`가 이미 쓰는 형태로 — 출시한 뒤 규칙이 명명한 세 형태 밖에 있다고 주장할 수도 있었습니다. 그 주장은 릴리스 일관성을 검사하고 증명 표면을 더하지 않는 `push-gate`에는 성립합니다. 여기서는 성립하지 않습니다: 변이 검사기는 어떻게 읽어도 증명용 비계이며, 좁은 문자적 해석으로 규칙을 우회하는 것은 이 저장소 자신의 감사가 그 코드에서 잡아낸 바로 그 대체 행위의 거버넌스 판본이 될 것입니다.

이 예외가 사들이는 것: rc.6 프로그램은 증명이 한 번도 작성되지 않은 가드를 두 번 착지시켰고, 그 결과 그 가드를 되돌려도 아무것도 빨간불이 되지 않았습니다. 그 시정은 커밋 메시지에 기록된 규율이었습니다 — 각 가드를 되돌리고, 빨간불을 관측하고, 복원한다. 이것은 그 규율을 기계 판정으로 만듭니다. 새로운 역량을 선언하지 않고, 기존 증명이 여전히 무는지를 시험합니다.

범위: `guard-check`는 `push-gate`에 연결된 독립 실행형 스크립트로 남습니다. 이것은 의도적으로 `verify:p1`에 **접히지 않습니다**. 각 항목이 빌드 한 번에 테스트 실행 한 번(측정값 약 4-5초)을 소모하고, 레지스트리의 열여덟 개 항목은 P1 벽시계 시간을 "모든 변경마다 실행한다"는 규율을 보호하는 180초 에스컬레이션 트리거 너머로 밀어붙일 것이기 때문입니다.

**현재.** `{proofLines: 26631, productLines: 16011, ratio: 1.6633}`, 2026-07-20 측정. **이 숫자를 인용하지 말고 다시 측정하십시오.** 이것은 핀이 아니라 스냅샷입니다: 이미 한 번 144줄만큼 낡은 것으로 드러났고, 여기 실려 있던 이어지는 논평의 모든 항목은 다음 변경이 착지하는 순간 낡아버렸습니다 — 어떤 작업이 "마지막" 줄을 더했는지 적은 문단은 그보다 나중 것이 생기는 순간 틀립니다. 비율은 rc.6 프로그램, OD-21 가드 검사기, 릴리스 후 하드닝, OD-16 중복 제거 작업, 그리고 `host-evidence`를 거치며 `1.535`와 `1.6575` 사이를 오갔습니다. **이 파일에 대한 git log가 곧 히스토리이며, 그것은 낡지 않습니다.** 여기서 중요한 것은 현재 값, 위의 규칙, 그리고 비율이 `1.0`에 결코 근접한 적이 없어 규칙이 여전히 구속력을 가진다는 사실입니다.

**측정.** 보고 전용 명령을 실행하십시오:

```sh
node scripts/task.mjs budget
```

이것은 reason `PROOF_BUDGET_REPORT`와 함께 `{proofLines, productLines, ratio}`를 출력하고 항상 `0`으로 종료합니다. 이 명령은 게이트가 아니라 측정입니다: 예산 규칙이 스스로 관장하는 종류의 게이트를 더하지 않도록, 의도적으로 검증 맵과 지속적 통합(CI)에서 빠져 있습니다. 이 규칙은 리뷰어와 Owner 게이트를 구속하지, CI를 구속하지 않습니다.

## 증거는 게이트가 아니다 — `pnpm host-evidence` (OD-C3, 2026-07-20)

`scripts/host-evidence.mjs`는 실제 Claude Code 바이너리를 어댑터 페이로드의 실제 설치본을 상대로 구동하고 `docs/verification/host/claude-code.json`을 씁니다. 이것은 **검증 게이트가 아니라 릴리스 증거**이며, 그 구분이 이것에 관한 세 가지를 결정합니다:

- 이것은 `verify:*` 네임스페이스에 있지 **않고**, 검증 맵에 있지 **않으며**, 어떤 게이트나 CI 작업도 이것에 의존하지 **않습니다**. 따라서 새 게이트에 대한 예산 규칙의 금지는 발동되지 않으며 어떤 예외도 필요하지 않았습니다.
- 이것은 Claude Code가 없는 곳에서는 실행될 수 없습니다. 아무도 재현할 수 없는 검사는 모두가 건너뛰기를 배우는 검사가 되며, 그것이 게이트가 거짓말을 시작하는 방식입니다.
- 이것의 **부재는 릴리스를 막지만**, 이것의 종료 코드는 아무것도 막지 않습니다. 그 둘은 서로 다른 메커니즘이며, 의도적으로 같은 하나로 표현되지 않습니다.

영수증은 두 그룹으로 나뉘어 작성되는데, 서로 다른 실행자가 필요하기 때문입니다. 그룹 A는 자격 증명 없이 관측 가능합니다 — 훅은 인증 전에 발화하므로, 401에서 죽는 샌드박스 세션도 그것들을 이미 실행한 것입니다. 그룹 B는 인증된 세션이 필요하며 Owner가 실행할 몫입니다. **그룹 B가 실행되지 않았을 때 영수증은 그것을 생략하는 대신 부재로 표시해야 합니다**: 검사된 것만 나열하는 영수증은 완전한 것처럼 읽히고, 그룹 A가 초록불이 되는 것은 그렇지 않았다면 전체로 오인될 바로 그 결과이기 때문입니다.

그룹 B는 재구성해야 할 절차가 아니라 두 개의 명령입니다:

```sh
pnpm host-evidence --prepare-group-b     # installs a probe, prints what to run
# run the printed `claude -p …` in the probe, then:
pnpm host-evidence --record-group-b --observed "<the answer>" --runner "<who>"
```

출력된 명령은 그 프롬프트를 stdin으로 파이프해 넣습니다. `--tools`는 가변 인자를 받으므로, 그 뒤에 쓴 프롬프트는 또 다른 도구 이름으로 소비되고 CLI는 "Input must be provided"라며 거부합니다 — 이것의 첫 버전이 그런 식으로 출시되었는데, 그 플래그를 `--help`에서 확인했을 뿐 조합된 명령을 실제로 실행한 적은 없었기 때문입니다.

질문은 모델에게 그 세션 컨텍스트가 어떤 workspace id를 언급하는지 묻고, 그 답이 곧 관측입니다 — 그래서 `--record-group-b`는 판정을 받아들이는 대신 그것을 설치된 id와 대조합니다. 그것을 지목하지 않는 응답은 조용히 버려지지 않고 `CONTRADICTED`로 기록되며, 실행자의 이름은 결과와 나란히 영수증에 들어갑니다.

**두 가지 성질이 그 답을 우연이 아니라 증거로 만들며, 둘 다 필수입니다.** workspace id는 준비할 때마다 새로 발행되는 nonce이므로, 프로브의 경로나 모델이 이전에 본 어떤 것으로부터도 추측할 수 없습니다. 그리고 출력된 명령은 `--tools ""`를 넘기므로, id는 `project.json`에서 읽어낼 수 없습니다 — 그 파일은 바로 거기 놓여 있고 제거할 수도 없는데, 핸들러가 그것을 읽기 때문입니다. 둘 중 하나라도 빼면, 올바른 답은 요약이 애초에 모델에 전혀 도달하지 않은 상황과도 양립하게 되는데, 그것이야말로 이 관측이 확립하려고 존재하는 단 하나의 사실입니다.

그룹 A 실행은 영수증을 다시 쓰지만, **기록된 그룹 B를 초기화하는 대신 앞으로 이월하며**, 그것을 더 이른 바이트에 대해 취해진 것으로 표시합니다. 그룹 B는 사람에게 한 세션을 치르게 합니다; 그룹 A를 재생성하는 일이 그것을 조용히 소진할 수 있어서는 결코 안 됩니다. 명시된 낡은 provenance는 회복 가능하지만 — 관측이 있던 자리의 공백은 그렇지 않습니다.

## 문서화와 번역

사람이 읽는 루트 문서들은 `scripts/policy/doc-coverage.json`에 선언된 언어들로 미러링되며 `docs/style/house-style.md`의 하우스 스타일을 따릅니다. 영어가 권위 있는 버전입니다; 각 번역은 그 영어 소스의 SHA-256을 고정합니다. 이 문서들 중 하나를 변경할 때는, 같은 변경 안에서 모든 번역을 다시 동기화하고 다시 고정하십시오 — `pnpm push-gate`는 낡은 핀, 누락된 번역, 산문에 남겨진 버전, 또는 CJK 강조 규칙에 대해 페일클로즈합니다. `LICENSE`, `NOTICE`, `CHANGELOG.md`와 `SUPPORT.md`는 정책상 영어 전용입니다.

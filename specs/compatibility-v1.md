# Compatibility V1

Compatibility documents conform to `compatibility-v1.schema.json`. Version
windows are inclusive and invalid windows fail with `VERSION_WINDOW_INVALID`.
Maturity is only `specified` or `fixture_verified` in P2.

No P2 document asserts a supported live release pair. Such an assertion
requires later runtime evidence and an accepted release boundary.

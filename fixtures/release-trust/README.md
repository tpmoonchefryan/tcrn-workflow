# Release-trust fixtures

Tests generate ephemeral Ed25519 keys, external trust roots, bundles, and
negative mutations in an operating-system temporary directory. No trusted key
or release authority is committed to the candidate repository.

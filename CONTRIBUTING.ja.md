<!-- tcrn-doc-synced-to: CONTRIBUTING.md b3ab92e983b0fa51919e7e4f1b12cbfbda751b7193f6eead4ab4083550ad7b0f -->

> **英語版が正本です。** この翻訳は便宜のために提供されるものであり、両者が異なる場合は [CONTRIBUTING.md](./CONTRIBUTING.md) にある英語のテキストが優先されます。

[English](./CONTRIBUTING.md) · [简体中文](./CONTRIBUTING.zh-CN.md) · 日本語 · [한국어](./CONTRIBUTING.ko.md) · [Français](./CONTRIBUTING.fr.md)

# コントリビューション

固定された Node と pnpm のバージョンを使用してください。パッケージのライフサイクルスクリプトを有効化したり、固定されていない実行可能ファイルを追加したり、テレメトリを導入したり、プロジェクトコマンドが暗黙のうちにネットワークへアクセスするようにしたりしてはなりません。

変更を提案する前に：

```sh
pnpm install --offline --frozen-lockfile --ignore-scripts
pnpm verify:p1
```

公開ブランチを push する前に `pnpm preflight` を実行してください。これは独立した
`git clone --no-local --no-hardlinks` を作成し、非公開の `TCRN_*` と兄弟チェックアウトの
環境変数を除去したうえで、P1 と lessons のゲートを fail-fast なしで実行し、すべての
赤い脚を同じレシートに記録します。プローブ規律は機械で検査されます：zsh のコマンド修飾子、
シェルの結合、`PIPESTATUS`、`ref:path` プローブ、`tail`、`head`、`grep` を判定に使わないでください。
`pnpm verify:privacy` はチェックアウトした HEAD から到達可能な公開面を判定し、
`pnpm verify:privacy:history` は別のローカル診断です。保持された過去のタグに廃止済みのバイトが
含まれる場合、後者は赤のままでも構いません。

依存関係は厳密なバージョンでなければならず、Apache-2.0 での配布と互換性があり、オフライン依存ポリシーおよび脆弱性ポリシーに追加されていなければなりません。コメントを受け付けるソースファイルには `SPDX-License-Identifier: Apache-2.0` を含めなければなりません。

リリースの挙動は、外部信頼ルートが不在、候補が制御している、期限切れ、失効、または署名済みマニフェストと不整合である場合に、フェイルクローズしなければなりません。

## 証明予算

**定義。** 証明質量とは、`tests/**/*.mjs` と `scripts/**/*.mjs` の改行数の合計です（`.mjs` フィルターは `scripts/policy` の JSON ポリシーファイルを除外します）。プロダクト質量とは、`packages/*/src/**/*.ts` の改行数の合計です。空行とコメントは意図的にカウントされます：この尺度はわざと粗くしてあり、そうすることで決定的になり、再フォーマットをめぐる議論の余地がなくなります。証明対プロダクト比は `proofMass / productMass` です。

**規則。** 比が `1.0` 以上である間は、いかなるプルリクエストも新たな検証ゲート——すなわち新たな `scripts/task.mjs` ハンドラ、新たな `verify:*` スクリプト、またはカテゴリが `framework-hygiene` である新たな検証マップのクレーム——を導入してはなりません。ただし、同じプルリクエストが少なくとも同等の証明質量を退役させるか、Owner が書面による例外を記録する場合を除きます。カテゴリが `runtime-capability` であるクレームは除外されます：それらは証明の足場ではなく、プロダクトが自らの仕事をしているものだからです。

**ベースライン。** 採用時点で測定された比はおよそ `1.62` でした（修正済みベースライン。定義に従い `packages/protocol` パッケージをプロダクト質量に含めています）。これは `1.0` のしきい値を大きく上回るため、規則は拘束力を持ちます。

**機械による強制 — 2026-08-19, TCRN-CROSS-STORY-301。** この日付まで、上記の規則は
拘束していながら何も判定していませんでした：`pnpm verify:budget` は比を報告して無条件に
成功を返していたため、規則は覚えている者によってのみ守られ、その間に比は `1.62` から
`1.59` へ動き、追加されたゲートは規則が挙げる三つの帰結のいずれも取っていません。
この動詞は現在、`scripts/policy/proof-budget.json` に記録された線を超える比を拒否し、
P1 の中で実行されます。

この検査は代理指標であり、そのことを明記しています。規則が語るのはゲートの導入であり、
検査が測るのは proof の量が、それが証明する製品より速く増えたかどうかです。したがって
一方向には厳しく——真に新しい能力のために書かれたテストでも触れうる——もう一方向には
盲目です：製品コードを削れば proof を一行も増やさずに比が上がります。どちらも取り繕わず
受け入れます。代替案はスクリプトに「何がゲートか」を判定させることであり、その判定こそが
一年間なされなかったものだからです。

線が動くのは、認める比とその理由を記した項目によってのみです。したがってポリシーファイルは、
これが支払われたすべての履歴として読めます。

**記録された例外 — OD-22, 2026-08-19, ラチェット自身の導入。** ラチェットが最初にした
ことは、それを導入した変更を拒否することでした：この動詞の論拠は `scripts/` にあり、その
判定基準は `tests/` にあり、上記の定義では両方とも proof の量です。proof の量の増加を
止める仕組みは、いくらか増やさずには導入できません。この許容量は凍結線の編集で吸収せず
記録します——導入直後に自らの上限をひそかに引き上げる仕組みは、それが止めるべき習慣を
そのまま教えてしまうからです。

**記録された例外 — OD-21, 2026-07-19, `pnpm guard-check`。** Owner は、ガードレジストリとその変異チェッカー（`scripts/guard-check.mjs`、`scripts/policy/guard-registry.json`）に対して一つの書面による例外を認めます。

この例外は回避されるのではなく、ここに記録されており、その区別には意味があります。このチェッカーは、スタンドアロンの npm スクリプトとして——`push-gate` がすでに用いている形で——出荷し、その上で規則が名指しする三つの形態の外側に該当すると論じることもできたはずです。その論法は `push-gate` には成り立ちます：`push-gate` はリリースの整合性を検査し、証明面を一切増やさないからです。ここでは成り立ちません：変異チェッカーはどう読んでも証明の足場であり、狭い字義的解釈によって規則を迂回することは、このリポジトリ自身の監査がそのコードの中で捉えた、まさにその置換の統治版になるでしょう。

この例外が買うもの：rc.6 プログラムは、証明が一度も書かれなかったガードを二度着地させ、その結果、そのガードを差し戻しても何も赤くなりませんでした。是正は、コミットメッセージに記録された規律でした——各ガードを差し戻し、赤を観測し、復元する。この例外はその規律を機械の判断にします。新たな能力を宣言するものではありません；既存の証明がまだ噛みつくかどうかを検査するものです。

スコープ：`guard-check` は `push-gate` に組み込まれたスタンドアロンのスクリプトのままです。これは意図的に `verify:p1` へ**折り込まれていません**。各エントリはビルドとテスト実行で（実測で約 4〜5 秒）コストがかかり、レジストリの十八のエントリは P1 の実時間を、「変更のたびに実行する」という規律を守る 180 秒のエスカレーショントリガーを超えて押し上げてしまうからです。

**現在値。** `{proofLines: 26631, productLines: 16011, ratio: 1.6633}`、2026-07-20 に測定。**この数字を引用するのではなく、測定し直してください。** これはスナップショットであってピンではありません：すでに一度、144 行分だけ陳腐化していたことが判明しています。そしてかつてここに書かれていた実況コメントの各項目は、次の変更が着地した瞬間に陳腐化しました——どの作業が「最後の」行を追加したかを述べる段落は、より後の変更が現れた途端に誤りになります。比は、rc.6 プログラム、OD-21 のガードチェッカー、リリース後のハードニング、OD-16 の重複除去作業、そして `host-evidence` を通じて、`1.535` から `1.6575` の間を動いてきました。**このファイルに対する git log が履歴であり、それは陳腐化しません。** ここで重要なのは、現在値と、上記の規則と、比が一度も `1.0` に近づいたことがない——したがって規則は依然として拘束力を持つ——という点です。

**測定。** レポート専用のコマンドを実行します：

```sh
node scripts/task.mjs budget
```

これは `{proofLines, productLines, ratio}` を reason `PROOF_BUDGET_REPORT` とともに出力し、常に `0` で終了します。このコマンドはゲートではなく測定です：検証マップからも継続的インテグレーションからも意図的に外されており、それはまさに、予算規則がそれ自身の統治するたぐいのゲートを追加してしまわないためです。この規則は CI ではなく、レビュアーと Owner ゲートを拘束します。

## 証拠はゲートではない — `pnpm host-evidence`（OD-C3, 2026-07-20）

`scripts/host-evidence.mjs` は、実機の Claude Code バイナリを、アダプターペイロードの実際のインストールに対して駆動し、`docs/verification/host/claude-code.json` を書き出します。これは**検証ゲートではなくリリース証拠**であり、その区別が以下の三つのことを決めます：

- これは `verify:*` の名前空間に**なく**、検証マップにも**なく**、いかなるゲートも CI ジョブもこれに依存**しません**。したがって予算規則の新規ゲート禁止は発動せず、例外も必要ありませんでした。
- これは Claude Code が存在しない環境では実行できません。誰も再現できないチェックは、誰もがスキップすることを覚えるチェックになります——それがゲートが嘘をつき始める仕組みです。
- その**不在はリリースをブロックします**；その終了コードは何もブロックしません。これらは異なる機構であり、意図的に同一の機構では表現されていません。

レシートは、異なるランナーを必要とするため二つのグループに分けて書かれます。グループ A はクレデンシャルなしで観測可能です——フックは認証の前に発火するため、401 で死ぬサンドボックスセッションでもそれらを実行し終えています。グループ B はクレデンシャルを伴うセッションを必要とし、実行するのは Owner の役目です。**グループ B が実行されていないとき、レシートはそれを省略するのではなく不在として示さなければなりません**：チェックされたものだけを列挙するレシートは完全であるかのように読まれ、そしてグループ A が緑になることは、さもなければ全体と取り違えられかねない、まさにその結果だからです。

グループ B は二つのコマンドであり、再構築すべき手順ではありません：

```sh
pnpm host-evidence --prepare-group-b     # installs a probe, prints what to run
# run the printed `claude -p …` in the probe, then:
pnpm host-evidence --record-group-b --observed "<the answer>" --runner "<who>"
```

表示されるコマンドは、そのプロンプトを stdin でパイプ入力します。`--tools` は可変長引数なので、その後ろに書かれたプロンプトは別のツール名として消費され、CLI は「Input must be provided」として拒否します——この最初のバージョンはそのように出荷されました。フラグは `--help` で確認され、組み立てられたコマンドが実際には一度も実行されなかったからです。

この問いは、モデルに対して、そのセッションコンテキストがどのワークスペース id に言及しているかを尋ね、その答えが観測です——だからこそ `--record-group-b` は、判定を受け入れるのではなく、インストール済みの id と照合します。それを名指ししない返答は、静かに捨てられるのではなく `CONTRADICTED` として記録され、ランナーの名前が結果の傍らでレシートに入ります。

**二つの性質が、その答えを偶然ではなく証拠にしており、その両方が必須です。** ワークスペース id は準備ごとに鋳造される nonce なので、プローブのパスからも、モデルがそれ以前に見たいかなるものからも推測できません。そして表示されるコマンドは `--tools ""` を渡すため、id を `project.json` から読み出すことはできません——そのファイルはまさにそこにあり、ハンドラがそれを読むので、取り除くこともできません。どちらか一方でも欠ければ、正しい答えは、サマリーがモデルにまったく到達していなかったことと両立するようになります——それこそが、この観測が確立するために存在する唯一のことなのです。

グループ A の実行はレシートを書き換えますが、**記録済みのグループ B をリセットするのではなく前方へ引き継ぎ**、それをより古いバイトに対して取得されたものとして印付けます。グループ B は人間にセッション一回分のコストをかけます；グループ A を再生成することが、それを静かに費やせてしまってはなりません。陳腐化した provenance が明記されていれば回復可能です——観測がかつてあった場所の空白は、そうではありません。

## ドキュメントと翻訳

人間向けのルートドキュメントは、`scripts/policy/doc-coverage.json` で宣言された言語にミラーされ、`docs/style/house-style.md` のハウススタイルに従います。英語が正本です；各翻訳はその英語ソースの SHA-256 をピン留めします。これらのドキュメントのいずれかを変更するときは、同じ変更の中ですべての翻訳を再同期し、再びピン留めしてください——`pnpm push-gate` は、陳腐化したピン、欠落した翻訳、散文中に取り残されたバージョン、または CJK 強調規則に対してフェイルクローズします。`LICENSE`、`NOTICE`、`CHANGELOG.md`、および `SUPPORT.md` は、ポリシーにより英語のみです。

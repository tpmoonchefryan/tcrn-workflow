<!-- tcrn-doc-synced-to: PRIVACY.md 6f95447d986dc694988233e433650c82fd3c76d8dfc74c27db7a0ab08d7f39bf -->

> **英文版本为权威版本。** 本翻译仅供参考；如两者有出入，以 [PRIVACY.md](./PRIVACY.md) 的英文文本为准。

[English](./PRIVACY.md) · 简体中文 · [日本語](./PRIVACY.ja.md) · [한국어](./PRIVACY.ko.md) · [Français](./PRIVACY.fr.md)

# 隐私

TCRN Workflow 不含任何遥测客户端。P1 项目命令在运行时带有一道 Node 进程网络守卫、一份精确的子进程白名单、离线的包管理器默认设置，以及针对有网络能力的导入与工具的静态检查。这不是一个内核或操作系统级的网络沙箱。CI action 启动以及被明确标记为冻结的依赖获取步骤，仍然是外部网络边界。

源码与发布归档中不得包含凭据、个人数据、机器特定路径、私有控制平面记录、运行时状态、原始邮箱地址、客户/源导出或对话标识符。公开的 Git 仓库 URL、公开的 Git 托管用户名，以及与之匹配的 GitHub 生成的 noreply 地址，仅在 Git 提交或注解标签元数据需要它们的地方才被允许。它们不会仅仅因为是公开的，就被允许出现在源码、文件名、归档或提交信息中。

`pnpm verify:privacy` 扫描当前的文件名与内容、源码归档、所有存储的 Git commit/tree/blob/tag 对象、从每一个可达的提交根出发的完整递归路径，以及 ref 元数据。读取失败即关闭；要求单链的常规源输入。生成的输出被 Git 忽略，但只会在持有干净 checkout 的独占输出会话期间，通过一个经校验的真实 `dist` 根写出。预先存在的重定向失败即关闭；并发的敌意工作区变更不在 P1 威胁模型之内。

扫描器是一项聚焦的确定性正则策略，而不是一个通用的 DLP 系统。一次新鲜的通告或 Codex Security 扫描，以及一个操作系统网络沙箱，仍然是外部边界。

P8 候选包只包含规范化源码归档、发布清单、校验和、SBOM、provenance 和发布说明。本地信任测试密钥留在候选 checkout 之外，绝不会被拷贝进这些制品。

P2 仅允许唯一一个精确的公开控制平面字符串作为规范契约：`.context/platform/workflow-v3-capabilities/p3-local-work-graph.accepted.json`。标记本身在 P2 中不存在，其存在会被 P2 验证拒绝。不允许任何同级控制平面路径或嵌入的控制平面内容。

任何未来的联网特性都需要一份显式的选择加入契约、记录在案的数据流、保留策略，以及单独的验收。

<!-- tcrn-doc-synced-to: CONTRIBUTING.md 3e2f9b9f05b1b55b1518b5de2b17887ec6f1076d7b02dfc9ba89ae457dedd6f2 -->

> **英文版为权威版本。** 本翻译仅供参考；如有出入，以 CONTRIBUTING.md 的英文文本为准。

# 参与贡献

使用钉定的 Node 与 pnpm 版本。不要启用软件包生命周期脚本、不要添加未钉定的可执行文件、不要引入遥测，也不要让项目命令隐式访问网络。

在提出变更之前：

```sh
pnpm install --offline --frozen-lockfile --ignore-scripts
pnpm verify:p1
```

推送公开分支前运行 `pnpm preflight`。它会创建独立的
`git clone --no-local --no-hardlinks`，清除私有 `TCRN_*` 与兄弟检出环境变量，
并以不快速失败的方式运行 P1 与 lessons 门，将同一回执中的全部红腿都显式列出。
探针纪律由机器执行：不得使用 zsh 命令修饰符、shell 连接符、`PIPESTATUS`、
`ref:path` 探针、`tail`、`head` 或 `grep` 作为判定。`pnpm verify:privacy` 判定已检出
HEAD 可达的公开面；`pnpm verify:privacy:history` 是独立的本地诊断，如果保留的历史
标签包含已退役字节，它可以继续为红。

依赖必须是精确版本，与 Apache-2.0 分发兼容，并被加入到离线依赖策略与漏洞策略中。接受注释的源文件必须包含 `SPDX-License-Identifier: Apache-2.0`。

当外部信任根缺失、由候选方掌控、已过期、已吊销，或与签名清单不一致时，发布行为必须失败即关闭。

## 证明预算

**定义。** 证明体量是 `tests/**/*.mjs` 加 `scripts/**/*.mjs` 的换行符总数（`.mjs` 过滤会排除 `scripts/policy` 下的 JSON 策略文件）。产品体量是 `packages/*/src/**/*.ts` 的换行符总数。空行与注释是刻意计入的：这个度量是有意做得粗糙的，好让它保持确定性，并且不给重新格式化留下争论空间。证明与产品之比是 `proofMass / productMass`。

**规则。** 只要该比值处于 `1.0` 或以上，任何 pull request 都不得引入一道新的验证门——也就是一个新的 `scripts/task.mjs` 处理器、一个新的 `verify:*` 脚本，或一条类目为 `framework-hygiene` 的新验证映射声明——除非同一个 pull request 至少退役等量的证明体量，或者 Owner 记录一条书面例外。类目为 `runtime-capability` 的声明豁免：它们是产品在履行其职责，而不是证明脚手架。

**基线。** 采用之初实测比值约为 `1.62`（修正后的基线，按其定义把 `packages/protocol` 包计入产品体量），远高于 `1.0` 阈值，因此该规则具有约束力。

**记录在案的例外 —— OD-21，2026-07-19，`pnpm guard-check`。** Owner 为守卫登记表及其变异检查器（`scripts/guard-check.mjs`、`scripts/policy/guard-registry.json`）授予一条书面例外。

这条例外是被记录下来，而不是被回避掉的，而这个区别很重要。这个检查器本可以作为一个独立的 npm 脚本发布——就是 `push-gate` 已经采用的那种形态——然后被论证为落在该规则三种具名形式之外。那套论证对 `push-gate` 成立，它检查发布一致性，不增加任何证明面。但在这里不成立：变异检查器无论怎么读都是证明脚手架，而基于狭隘的字面解读绕过规则，就会是本仓库自己的审计在其代码中所抓到的那种替换在治理层面的形式。

这条例外换来的是：rc.6 program 曾两次落地一个从未写过证明的守卫，其后果是回退该守卫什么都不会变红。当时的纠正是一条记录在提交信息里的纪律——回退每一个守卫、观察其变红、再恢复。这条例外把那条纪律变成了一次机器判断。它不声明任何新能力；它检验既有证明是否仍然在咬。

范围：`guard-check` 保持为一个接入 `push-gate` 的独立脚本。它被刻意**不**折叠进 `verify:p1`，因为每一个条目都要付出一次构建加一次测试运行的代价（实测约 4-5 秒），而登记表的十八个条目会把 P1 挂钟时间推过 180 秒的升级触发阈值——那个阈值保护的正是“每次改动都跑一遍”的纪律。

**当前值。** `{proofLines: 26631, productLines: 16011, ratio: 1.6633}`，测量于 2026-07-20。**请重新测量，而不要引用这个数字。** 它是一个快照，不是一个钉子：它曾经已经被发现过时了 144 行，而曾经写在这里的那段实时评述里的每一条，都在下一次改动落地的那一刻变得过时——一段说哪项工作添加了“最后”那些行的话，一旦有了更晚的一项就是错的。在 rc.6 program、OD-21 守卫检查器、发布后加固、OD-16 去重工作以及 `host-evidence` 期间，这个比值在 `1.535` 与 `1.6575` 之间移动过；**这个文件上的 git log 才是历史，而它不会过时。** 这里要紧的是当前值、上面那条规则，以及这个比值从未逼近过 `1.0`，因此该规则仍然具有约束力。

**测量。** 运行这条仅报告命令：

```sh
node scripts/task.mjs budget
```

它会打印 `{proofLines, productLines, ratio}`，并带有 reason `PROOF_BUDGET_REPORT`，并且总是以 `0` 退出。这条命令是一次测量，不是一道门：它被有意地排除在验证映射与持续集成之外，正是为了让预算规则本身不去添加它所治理的那种门。该规则约束的是评审者与 Owner 门，而不是 CI。

## 证据不是门 —— `pnpm host-evidence`（OD-C3, 2026-07-20）

`scripts/host-evidence.mjs` 用真实的 Claude Code 二进制文件，针对适配器载荷的一次真实安装来驱动，并写出 `docs/verification/host/claude-code.json`。它是**发布证据，不是验证门**，而这个区别决定了关于它的三件事：

- 它**不**在 `verify:*` 命名空间里，**不**在验证映射里，也**没有**任何门或 CI 作业依赖它。因此预算规则对新门的禁令不会被触发，也不需要任何例外。
- 它无法在没有 Claude Code 的地方运行。一个没人能复现的检查会变成一个人人都学会跳过的检查，而这正是一道门开始说谎的方式。
- 它的**缺失会阻止一次发布**；它的退出码什么都不阻止。这是两种不同的机制，且被刻意地不用同一种机制来表达。

这份收据分两组写出，因为它们需要不同的运行者。A 组无需凭据即可观测——钩子在认证之前就触发，所以一个在 401 处死掉的沙箱会话仍然运行过它们。B 组需要一个带凭据的会话，且要由 Owner 来运行。**当 B 组还没有被运行时，收据必须把它显示为缺失，而不是把它省略掉**：一份只列出已检查内容的收据读起来像是完整的，而 A 组变绿恰恰是那个否则会被误当成全部的结果。

B 组是两条命令，而不是一套需要自行重建的流程：

```sh
pnpm host-evidence --prepare-group-b     # installs a probe, prints what to run
# run the printed `claude -p …` in the probe, then:
pnpm host-evidence --record-group-b --observed "<the answer>" --runner "<who>"
```

打印出来的命令会把它的提示词通过 stdin 管道输入。`--tools` 是变参的，所以写在它后面的提示词会被当作另一个工具名吞掉，于是 CLI 会以“Input must be provided”拒绝——这个东西的第一个版本就是那样发布的，因为那个旗标是在 `--help` 里检查过的，而组合出来的命令从未被真正运行过。

这个问题问的是模型：它的会话上下文提到的是哪个工作区 id，而答案就是那次观测——这正是为什么 `--record-group-b` 会把它与已安装的 id 比对，而不是接受一个判定。一个没有把它说出来的回复会被记为 `CONTRADICTED`，而不是被悄悄丢弃，并且运行者的名字会连同结果一起写进收据。

**有两条性质让那个答案成为证据而不是巧合，两者缺一不可。** 工作区 id 是每次准备时新铸的一个 nonce，所以它无法从探针的路径、也无法从模型此前看到的任何东西里猜出来。而打印出来的命令传入了 `--tools ""`，所以这个 id 无法从 `project.json` 里读出来——那个文件就摆在那儿，且无法被移除，因为处理器要读它。去掉其中任意一条，一个正确的答案就会变得与“那份摘要根本从未抵达模型”相容，而这恰恰是这次观测存在的唯一目的所要确立的事。

一次 A 组运行会重写收据，但**它会把已记录的 B 组向前携带，而不是重置它**，并把它标记为针对更早字节所采集的。B 组要花掉一个人一次会话；重新生成 A 组绝不能够悄无声息地把它花掉。声明出来的过时 provenance 是可恢复的——而一处本该有观测、如今却空白的地方，则不可恢复。

## 文档与翻译

面向人类的根文档会被镜像为 `scripts/policy/doc-coverage.json` 中声明的各种语言，并遵循 `docs/style/house-style.md` 中规定的编写风格。英文版为权威版本；每一份翻译都会钉定其英文源文件的 SHA-256。当你改动这些文档中的任何一份时，必须在同一次改动中重新同步并重新钉定每一份翻译——`pnpm push-gate` 会在遇到过时的钉子、缺失的翻译、遗留在正文里的版本号，或违反 CJK 强调规则时失败即关闭。按照策略，`LICENSE`、`NOTICE`、`CHANGELOG.md` 和 `SUPPORT.md` 仅提供英文版。

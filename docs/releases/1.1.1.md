# TCRN Workflow 1.1.1

This append-only patch release preserves the v1.1.0 event, storage, and CLI
protocols.

## Repairs

- Recovery of a stale output-session lock now has a canonical-repository keyed
  in-process guard. A same-process contender receives the causal
  `OUTPUT_SESSION_RECOVERY_CONCURRENT` result while the durable inter-process
  claim and identity checks remain unchanged.
- The deterministic lifecycle regression proves loser causality, a clean
  terminal state, and immediate reacquisition.

The immutable v1.1.0 publication remains unchanged. This release is published
only after the exact commit reaches terminal-success CI and its new immutable
tag and assets are independently verified.

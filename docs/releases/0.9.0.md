# TCRN Workflow 0.9.0

`0.9.0` is the eleventh accepted release. It adds a governed route for moving a
workspace to a new path or a new machine, and it is a minor release because it
adds four public commands and one optional metadata field.

It does not change the event-chain storage version, transforms no event, alters no
existing receipt, removes no verb, and still declares `supportedAosReleases: []`.

**It carries one irreversible cost, stated up front rather than in a footnote:**
after `relocation-vacate` has committed on a workspace — including one whose
relocation is later ABORTED and that never moved a single byte — no
`0.8.0`-or-earlier engine can read that workspace at all. The first cut of this page
said the break landed at the adopt; measured against a `0.8.0` build, it lands at the
vacate, and the *Compatibility* section below has always said so. The headline was the
sentence that was wrong.

## The gap

The engine binds five absolute roots and refuses to read a control tree whose
stored root identities disagree with the live filesystem. That refusal is right —
it is what stops a stray copy becoming a second source of truth — but there was no
governed route through it. A workspace that had to move left the operator exactly
one option: hand-edit `roots`. Nothing anywhere recorded that it had happened, and
nothing could tell the two resulting trees apart afterwards.

The documented reason for that gap was also wrong. ADR 0002 and the backup runbook
both said root rebinding "requires the migration apply path V1 lacks, per OD-29".
OD-29 is the manifest-scope decision — ADR 0002's own sign-off section says so —
and the apply-path deferral is OD-7, which is about storage-version-2 chain
rewriting. Root rebinding rewrites no event and changes no storage version. Event
identity is `sha256` over `(schemaVersion, workspaceId, createdAt)` and
`(streamId, sequence)`; there is no path in it anywhere. The misattribution sat in
two documents for four months.

## Added

### Four verbs that move the binding, never the bytes

| Verb | Host | Mutates | Effect |
| --- | --- | --- | --- |
| `relocation-plan` | source | no | Read-only. Emits the hop's `relocationId` and its control manifest. |
| `relocation-vacate` | source | yes | Its ONLY effect is to kill the source. |
| `relocation-adopt` | target | yes | Binds the copied tree to this host. Idempotent. |
| `relocation-abort` | source | yes | Revives the source from the ledger's own `from`. |
| `relocation-inspect` | either | no | Read-only. Compares an address against its own ledger. Requires `--at`. |

`relocation-plan` is first in practice as well as in the table: a permit names the
exact `relocationId` of the hop it authorizes, and the plan is the only route to
that id that is guaranteed to agree with the vacate — the two share one preparation
function so they cannot drift. It also emits the control manifest, which after the
vacate commits is unobtainable at both addresses while `relocation-adopt` requires
its exact text.

The order — vacate, copy, adopt — is mechanically enforced. Copying first and
adopting is refused, because copy-first is the operator's natural instinct and
must fail closed rather than produce an unauthorized live target.

**The operator moves the bytes.** The engine gets no copy path and never will:
ADR 0002 rejected a destination-writing verb by name, and ADR 0003 records the
refusal again, with the alternatives named, so a later reader does not helpfully
add `--copy` back.

Between the vacate and the adopt, **zero addresses are alive** — not two. That is
the mechanized form of "it only works in one mode". The source refuses everything with
`WORKSPACE_RELOCATION_VACATED` **except the two diagnosis reads (`relocation-inspect`,
`lease-inspect`) and `relocation-abort`**; the copy refuses everything with
`WORKSPACE_RELOCATION_ADOPTION_REQUIRED` **except `relocation-inspect`**. The carve-outs
are load-bearing rather than incidental — `relocation-inspect` at the vacated source is
one half of the mandated close-out compare, and abort is the only exit — and the first
cut of this page stated the rule without them. The two refusals are different and both
are actionable.

### An append-only relocation ledger inside `workspace.json`

`relocations` is a tenth, **optional** metadata field. `schemaVersion` stays
`tcrn.workspace.v1` and `storageVersion` stays `1`. It is **absent** — not empty —
on every workspace that never relocates, so those files are byte-identical to the
ones `0.8.0` writes.

Because it lives inside the copied tree, a later re-copy of a vacated source is
born dead, and a copy of an adopted target dropped back at the old path is dead
too. Both directions fail safe. An external registry would have neither property,
which is why there is no registry.

`roots` is never rewritten: it stays the binding the workspace was created with,
and `activeBinding()` answers where it lives now.

### A per-invocation relocation authority

The gate-identity pins-track shape — an out-of-band document plus its digest on
the command line, read through the same TOCTOU-hardened reader every other
authority uses. A permit names `actorId`, `workspaceIds`, `destinations`, a `basis`
of `{version, headEventHash}`, and — the two scopes that make it per-invocation
rather than merely per-vintage — the exact `relocationId` and the exact `stage`
(`vacate`, `adopt` or `abort`). An `abort` permit, and only an `abort` permit, also
names the hop's `vacateCommitmentSha256`.

**Why the last two exist, stated because the first cut of this release shipped
without them and was wrong.** Relocation does not advance the event chain, so after
a hop the `{version, headEventHash}` basis is byte-identical to what it was before.
An adversarial review therefore drove `vacate → adopt → abort` twice from ONE
authority file, with no tampering and every call returning a success code, and left
three addresses all answering `status` as the live authority for one `workspaceId`.
The basis bounds DRIFT, not REUSE. A `relocationId` is derived over
`(workspaceId, sequence, from, to, basis)`, so a permit that names one authorizes
exactly one hop, and the `stage` term makes it exactly one verb of that hop.

Each scoping still stops a different thing, and each has its own red proof: without
`workspaceIds` the roster permits every workspace on the machine while looking
rigorous; without `destinations` a permit to move a workspace somewhere approved
also moves it anywhere else; without the `basis` a permit can name the right hop and
a stale vintage; without `relocationId` or `stage` it is a standing grant in
per-invocation clothes.

**The abort permit's vacate commitment, added in the third cut because the stage term
alone was measured as insufficient.** One authority document may legitimately carry a
`vacate`, an `adopt` and an `abort` permit for one hop, so a review minted exactly that
document BEFORE the vacate and forked the workspace from it — one file, one approval,
one extra flag. An `abort` permit now names `vacateCommitmentSha256`: the sha256 of the
committed `vacated` ledger entry, which itself contains the sha256 of the authority file
that authorised the vacate. A document carrying both permits for one hop would therefore
have to contain a digest of itself, which is not constructible. The value travels in the
vacate receipt and is restated by `relocation-inspect` at the vacated address, so abort
stays a pure function of the tree. The permanent consequence is readable in the ledger:
**the two entries of an aborted hop can never name the same authority file.**

What that buys is two documents and two approvals where there was one. **It is a review
device, not a barrier** — see *What this does NOT do*.

The three mutating verbs are **not** on the MCP surface. An MCP grant is a
standing command list, and a standing grant to take over a workspace is exactly
what a per-invocation authority forbids. The exclusion is derived from the command
catalog — any verb with a required `*-authority-digest` flag — not hand-listed,
because a list of names goes stale in the permissive direction.
`relocation-inspect` and `relocation-plan` are exposed; both are read-only and
neither can bind anything.

The exclusion is enforced on BOTH MCP surfaces, and it is tested on both. The
`tools/list` filter is not a backstop for `tools/call`: the dispatcher resolves a
tool name against the full catalog, so with the call-side clause removed a standing
grant naming `relocation-vacate` executes a workspace vacate while the verb stays
invisible in the listing. That clause shipped with no test and no guard; it now has
both, with the other side of the predicate asserted in the same test (an ordinary
mutating verb must reach the authority layer, or `MCP_TOOL_UNKNOWN` proves nothing).

### Two blindnesses the snapshot manifest structurally cannot cover

`walkControlTree` collects files only and skips four claim paths by design, so the
manifest cannot see a copy that **dropped an empty directory** and cannot see one
that **carried a lock file across**. Neither is a bug in the manifest; both are it
doing what it was scoped to do. `relocation-adopt` asserts on both, and the proof
for each asserts *both* halves: that `snapshot-verify` still returns
`SNAPSHOT_VERIFIED`, and that adopt then refuses under its own code. Without the
first half the second would be a check that cannot be shown to measure anything.

## Compatibility

Additive for every workspace that never relocates: identical `workspace.json`
bytes, identical receipts, no flag becomes required, nothing removed.

**For a workspace that carries a relocation ledger — including one whose relocation
was ABORTED and that never moved a byte — the break is total and one-way.** The
tenth metadata field fails the closed-field check with `WORKSPACE_SCHEMA_INVALID` on
any `0.8.0`-or-earlier binary — not degraded reads, no reads. The aborted case is
called out because it is the one an operator is most likely to hit and least likely
to expect: the ledger is append-only, so "I cancelled the move" also means "I
version-locked this partition". This is the WSD-1 cost
profile the engine has already absorbed twice, and it is accepted deliberately
rather than discovered: the residual-applicability analysis is in
`packages/core/spec/file-engine-v1.md` and ADR 0003. The old immutability rule
still holds for every field and every caller except the three relocation verbs,
which is a conditional retention rather than a deletion.

## What this does NOT do

`workspace.json` is the one part of the control tree the event hash chain does not
cover. Anyone with write access to a vacated source can restore its pre-vacate
`workspace.json` in canonical bytes and that address is fully alive again. The
engine cannot detect this — not "does not currently"; cannot. It is the same
ceiling `gate-identity.ts` already states about gates.

So this release **does not prevent two truths. It makes them legible** at the
addresses its ledger names: an `aborted` at the source and an `adopted` at the
target sharing one `relocationId` is a permanent contradiction in both files.

**Three claims that stood on earlier cuts of this page were false, and are replaced
rather than softened.**

1. It said producing a fork "now requires deliberately destroying an artifact
   rather than being an accident of `rsync`". It did not: `relocation-abort` alone
   produced one, destroying nothing, and abort is the documented recovery verb. The
   review ran `vacate → adopt → abort` with engine verbs only and got two live
   addresses accepting divergent writes. Abort now costs an authority minted for
   THIS hop's `abort` stage and naming this hop's `vacateCommitmentSha256`, an
   explicit `--acknowledge-fork-risk`, and — whenever the operator can reach the
   destination — a `--target-inspection` document. None of that
   makes the fork impossible: an offline source cannot know what the destination
   did, and the receipt and every later `relocation-inspect` say so in those words.
2. It presented the two-sided compare as *the* fork detector. It is not. A third
   address created by copying the tree, deleting `relocations` and rewriting `roots`
   is a fully live authority for the same chain, and it appears in no ledger — so a
   green two-sided close-out is not evidence that no fork exists. That hole is the
   pre-`0.9.0` hand-edit hole, which this design does not close and does not claim
   to; what was wrong was the sentence, not the mechanism.
3. The second cut said the `--target-inspection` document "the engine CHECKS … refuses
   the abort outright if the destination has already adopted". **The engine checks the
   DOCUMENT, and in that cut the document carried no observation instant at all** — one
   captured before the destination adopted was byte-identical to a fresh one, so the
   honest operator sequence (inspect the target, decide, abort) with an adopt landing in
   between passed the check and stamped `targetStateVerified: true` into the receipt.
   The document now carries a caller-declared `observedAt` (`relocation-inspect` requires
   `--at`), and abort refuses one that does not sit within one hour of the abort's own
   `at`, or that claims to have been observed after it, with
   `WORKSPACE_RELOCATION_INSPECTION_STALE`. **Both instants are caller-supplied: this
   bounds staleness and makes it statable; it is not a freshness proof and the check
   remains TOCTOU by construction.** The receipt no longer carries a field that reads as
   a verdict — it records `targetInspectionSupplied`, `targetInspectionSha256` (which
   document was shown) and `targetInspectionObservedAt` (what instant that document
   claimed).

The runbook step that no gate can replace stands, with its scope stated correctly:
**run `relocation-inspect` at BOTH addresses the ledger names and compare.** Exactly
one must report `live` for a given `workspaceId`. It proves those two addresses have
not both stayed live. It cannot see an address the ledger does not name. Treat it as
binding gate evidence for what it covers, and never as a proof of no-fork.

No single-sided "the source is still dead" assertion exists in the test suite, and
that absence is deliberate: such an assertion would be permanently true.

**Two further ceilings, stated with the same force as the two above because a third
review measured them and because both were previously implied away.**

- **The abort-stage permit is a legibility and review device, not a barrier.** Whoever
  can mint the vacate permit can mint the abort permit. Nothing authenticates a minter
  and nothing can observe when a document was minted, so "mint the abort permit only
  when you decide to abort" is counsel, not a control. The stage term and the vacate
  commitment force two documents and two approvals instead of one, and leave two
  different `authorityFileSha256` values in the ledger. An operator who intends to fork
  still forks, in two acts instead of one. **That is expected.**
- **A permit is a predicate over presented bytes at a path, not a token with a spend
  record.** Every input to `relocation-adopt` is host-neutral — the tree bytes, the
  manifest, the authority file, the actor, the instant and five path STRINGS — and the
  only record that a permit was spent is written into the copy the permit-holder
  controls. Present one shipped vacated tree at the destination path on N hosts and one
  adopt permit yields N simultaneously-live authorities for one `workspaceId`, all under
  the same `relocationId` — **and the mandated two-sided compare passes at every one of
  them.** It needs no hand-edit and produces no anomaly anywhere. Closing it needs a
  host-identifying term the ledger does not carry; that is not in `0.9.0`. Read a green
  close-out as a statement about the two addresses in front of you, never about the
  `relocationId`.

**The ledger cap is a one-way budget of eight attempts.** Sixteen entries, two per hop,
spent whether or not a byte moves — so eight vacate/abort cycles that relocate nothing
end a workspace's ability to relocate by any governed route, permanently. There is no
compaction verb and deliberately will not be one (ADR 0003 records why both available
shapes were rejected). `relocation-plan` now refuses at a full ledger BEFORE the minting
ceremony and reports `ledgerEntriesRemaining` and `hopsRemaining`; `relocation-inspect`
reports the same remaining count.

## Verification

`BK-RELOCATION-BINDING` is a new `runtime-capability` claim on the existing
`pnpm verify:backup` command — the proof-budget-exempt route, taken because it was
available and not because the rule was read narrowly. `BK-SNAPSHOT-WITNESS` loses
the clause that asserted a relocated restore raising `WORKSPACE_SCHEMA_INVALID` as
doctrine; the refusal itself is unchanged and still tested.

Guard-registry entries pin the parts that would otherwise fail silently: the strict
`readMetadata` admission default, the control-tree skeleton assertion, every term of
the permit match (`workspaceId`, `destinations`, `relocationId`, `stage`), the
address comparison in `relocationStateAt`, the abort address and admission layers
SEPARATELY, the adopt-side control-manifest binding, the ledger self-derivation and
completion-restatement rules, and the MCP dispatcher exclusion — which had no test
and no guard at all, and which is the entire boundary between a standing MCP grant
and the three workspace-takeover verbs.

{
  "schema": "tcrn.capability-manifest.v1",
  "purpose": "EPIC-021 minimal cross-host capability manifest, updated through INIT-009 EPIC-022/023/026 and INIT-010 EPIC-020. It records, per host, what surface each capability has, through what channel it is reached, and the governance level TCRN can honestly claim today. A level describes the shipped boundary, not whether an operator has installed or registered it in a particular host.",
  "observedAt": "2026-07-25",
  "governanceLevels": {
    "enforce": "TCRN can block or gate the capability before it takes effect",
    "observe": "TCRN records the capability fail-open, without blocking",
    "invoke-only": "the capability can be called, but TCRN cannot yet observe or gate it",
    "unavailable": "no TCRN integration exists for this capability on this host yet"
  },
  "evidence": {
    "codex": "current candidate: packages/core/src/codex-adapter-activation.ts and tests/act12-hook-root-binding.test.mjs; historical host observations remain in codex-0.139.0-facts.json and codex-live-integration-2026-07-25.json",
    "claude": "current candidate: packages/core/src/claude-adapter-activation.ts and tests/act12-hook-root-binding.test.mjs; historical host observations remain in claude-code.json and claude-code-facts.json"
  },
  "capabilities": [
    {
      "id": "session-start-context-injection",
      "claude": {
        "surface": "app",
        "channel": "hook:SessionStart",
        "governance": "observe",
        "note": "the current absolute-root definition is code- and fixture-proven only; the earlier claude-code.json observation predates these command bytes and is historical, so no current live claim is made"
      },
      "codex": {
        "surface": "app",
        "channel": "hook:SessionStart",
        "governance": "observe",
        "note": "the current machine-specific absolute-root definition is code- and hermetic-fire-proven only and awaits a fresh Owner /hooks review, approval and fire; July 25 receipts cover superseded command bytes and are not reused"
      }
    },
    {
      "id": "tool-lifecycle-observe",
      "claude": {
        "surface": "app",
        "channel": "hook:PostToolUse",
        "governance": "unavailable",
        "note": "surface exists; any hook beyond SessionStart is gated on the N-2 amendment (S072) before it may be added"
      },
      "codex": {
        "surface": "app",
        "channel": "hook:PostToolUse",
        "governance": "unavailable",
        "note": "observe handler exists, but Codex activation deliberately registers only SessionStart; PostToolUse remains gated per hook"
      }
    },
    {
      "id": "tool-approval-gate",
      "claude": {
        "surface": "app",
        "channel": "hook:PreToolUse/PermissionRequest",
        "governance": "unavailable",
        "note": "enforce-class; gated on S072 N-2 amendment and a failure-policy ruling"
      },
      "codex": {
        "surface": "app",
        "channel": "app-server:*Approval",
        "governance": "invoke-only",
        "note": "the App Server protocol exposes ExecCommand/ApplyPatch/FileChange/Permissions approvals; the shipped Observer never responds to them and no enforce gate exists"
      }
    },
    {
      "id": "thread-turn-control",
      "claude": {
        "surface": "n/a",
        "channel": "none",
        "governance": "unavailable",
        "note": "no measured equivalent control plane"
      },
      "codex": {
        "surface": "app",
        "channel": "app-server supplied notification stream",
        "governance": "observe",
        "note": "the read-only Observer and S054 collector bind supplied spawn/thread/turn/final-message notifications to session/thread/turn receipts. A real Codex Desktop 0.146.0-alpha.3.1 run now proves App-visible sub_agent_activity and completed subagent rollouts, but those rollout records are not the pinned App Server notification stream; no live attach or Workflow receipt comparison is claimed"
      }
    },
    {
      "id": "workflow-mcp-tools",
      "claude": {
        "surface": "cli",
        "channel": "mcp",
        "governance": "enforce",
        "note": "the host-neutral Workflow MCP server gates mutations through pinned operator authority; host registration remains an operator action"
      },
      "codex": {
        "surface": "cli",
        "channel": "mcp (codex mcp / mcp-server)",
        "governance": "enforce",
        "note": "the host-neutral server is registered and enabled, and the exact candidate completed direct initialize, tools/list and read-only status with pinned authority; mutations remain gated. The running Desktop process did not hot-load it and a fresh CLI host-routed attempt hit the account usage limit before a tool call"
      }
    },
    {
      "id": "adapter-activation",
      "claude": {
        "surface": "app",
        "channel": "installer+settings-fragment",
        "governance": "observe",
        "note": "the current absolute-root install/activate/uninstall path is code- and fixture-proven and byte-reversible; no current Claude live activation is claimed"
      },
      "codex": {
        "surface": "app",
        "channel": "installer+.codex/hooks.json+/hooks approval",
        "governance": "observe",
        "note": "install/deactivate are identity-gated, but the current absolute-root definition has not yet been installed, reapproved or fired. Installation alone never claims host activation, Codex's trust hash is opaque, and this exact-definition change requires reapproval"
      }
    }
  ],
  "sessionBinding": {
    "note": "the minimal binding a live session must carry, defined here as the contract EPIC-022's MCP admission and any observer receipt will populate. Attribution is not identity proof.",
    "requiredFields": [
      "hostProduct",
      "hostVersion",
      "capabilityManifestDigest",
      "workspaceId",
      "sessionId",
      "boundAt"
    ],
    "digestOf": "the canonical bytes of this manifest's `capabilities` array, so a session declares which capability baseline it was admitted against"
  },
  "compatibilityMatrix": {
    "note": "which surfaces each host exposes, and whether TCRN reaches them today. 'yes' means a surface exists to integrate; the governance level per capability above says whether that integration is built.",
    "rows": [
      {
        "surface": "CLI",
        "codex": "yes (codex, mcp, mcp-server)",
        "claude": "yes (claude, MCP)",
        "tcrnGoverned": "yes at the Workflow MCP boundary; host registration is operator-owned"
      },
      {
        "surface": "IDE",
        "codex": "unknown (not probed)",
        "claude": "yes (VS Code / JetBrains extensions)",
        "tcrnGoverned": "no"
      },
      {
        "surface": "App / desktop",
        "codex": "yes (codex app, app-server)",
        "claude": "yes (desktop / web)",
        "tcrnGoverned": "Claude and Codex SessionStart are code/hermetic observe capabilities for the current definitions, with live reapproval pending; Codex Desktop subagents are App-visible, while the App Server collector remains supplied-stream only"
      }
    ]
  },
  "notClaimed": [
    "no enforce-level host lifecycle or host-tool governance exists; enforce is limited to Workflow MCP mutations at the server boundary",
    "no host approval or internal trust hash is inferred from a Codex installation receipt",
    "no current Codex or Claude SessionStart definition is claimed live after the absolute-root command change",
    "no live App Server attach, Controller, or approval responder is claimed",
    "App-visible Codex Desktop multi-agent records exist but have not been converted to or compared with a Workflow execution receipt",
    "no hot-load into the running Codex Desktop process or successful host-routed Workflow MCP call is claimed",
    "governance levels are a snapshot of 2026-07-25 against Codex 0.139.0 and the measured Claude Code 2.1.201 activation"
  ]
}

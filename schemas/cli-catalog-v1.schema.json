{
  "$id": "https://tcrn.dev/schemas/cli-catalog-v1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TCRN CLI Command Catalog V1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "reasonCode",
    "schemaVersion",
    "commands"
  ],
  "properties": {
    "reasonCode": {
      "const": "CLI_CATALOG_READY"
    },
    "schemaVersion": {
      "const": "tcrn.cli-catalog.v1"
    },
    "commands": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "$comment": "INC-016: `mutates` and `authorityBearing` name two different authorization categories, and the operator authority bundle keeps their grant lists disjoint. One entry may not declare both, so declaring `authorityBearing` requires `mutates: false`.",
        "dependentSchemas": {
          "authorityBearing": {
            "properties": {
              "mutates": {
                "const": false
              }
            }
          }
        },
        "required": [
          "name",
          "availability",
          "mutates",
          "flags"
        ],
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "availability": {
            "enum": [
              "cli",
              "programmatic-only",
              "fixture-only"
            ]
          },
          "mutates": {
            "type": "boolean"
          },
          "authorityBearing": {
            "const": true
          },
          "flags": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "name",
                "required",
                "valueKind"
              ],
              "properties": {
                "name": {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9-]*$"
                },
                "required": {
                  "type": "boolean"
                },
                "valueKind": {
                  "enum": [
                    "string",
                    "integer",
                    "boolean",
                    "json",
                    "list",
                    "instant",
                    "enum"
                  ]
                },
                "nullSentinel": {
                  "const": "-"
                },
                "headSentinel": {
                  "const": true
                },
                "deprecatedAliases": {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}

# Support

The project is pre-release and community supported. Use a discussion for usage
questions and an issue for reproducible defects. Security reports follow
`SECURITY.md`.

Development mode is supported only with the pinned toolchain. Release mode is
supported only when the external trust-root verification contract succeeds.
